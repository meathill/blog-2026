# 博文草稿:TiDB 账单爆炸之后

> 元数据建议(发布时用):
> - **标题**:TiDB 账单爆炸之后:一次跌宕起伏的降本排查,从 Cloudflare 边缘一路查到数据库计费内核
> - **slug**:`tidb-ru-bill-explosion-investigation`
> - **分类**:tech
> - **标签**:TiDB, Cloudflare, WordPress, Headless CMS, 降本, APCu, Serverless
> - **摘要**:博客的 TiDB 账单逼近限额,RU 基线常年 110+。我和 Claude 用两天时间逐层排查:边缘缓存、漏洞扫描器、WordPress 对象缓存、连接税……每个假设都被数据打脸又重建,最后用一个「静默实验」逼出了真相。附全部实测数据与方法论。
> - 配图位置用 `[图:...]` 标出,发布前替换成截图。

---

我的博客架构是 Next.js(Cloudflare Workers)+ WordPress 当 headless CMS,WP 的数据库跑在 TiDB Cloud Starter 上。六月初我发现 TiDB 的账单要爆了:RU(Request Unit)基线常年 110–120/秒,月消耗 250M+,而免费额度只有 50M,$15 的 spending limit 眼看就要触顶——触顶的后果是限流,网站会变慢甚至超时。

我的第一反应和大多数人一样:**肯定是有人在刷我的 API**。把非必要请求都挡掉、加上缓存,问题应该就解决了。

后来的两天证明,这个直觉只对了一小半。整个排查像剥洋葱,每一层都藏着一个意外。

## 第一层:边缘收口,结果发现大门早就关了

计划很直接:blog 子域只保留 `/wp-json`,其它全部挡掉;给 `/wp-json` 配边缘缓存。

动手前先盘点现状,第一个意外就来了:**该挡的其实早就挡了**。之前做 SEO 收敛时,旧博客域名的几乎所有路径都已经在边缘 301 到主站,wp-json 也已经整体放在 Cloudflare Access 后面(service token,只有我自己的 Worker 能调用)。bot 根本进不来。

那钱花哪儿了?盘点倒是揪出一个真凶:Caddy 的 `php_fastcgi` 有个经典坑——**try_files 会把不存在的文件回落到 index.php**。也就是说,扫描器随便探测一个不存在的图片路径,都会启动整个 WordPress、打一串数据库查询。实测一个缺失图片的请求要 15 秒才超时。修复方式是把 `/wp-content`、`/wp-includes` 拆成纯 `file_server`,缺失文件 4ms 静态 404,零 PHP 零 DB。

顺手还发现 RSS feed 自从迁移到 OpenNext 之后就一直是 500——`runtime = 'edge'` 的 route 根本不会进构建产物。这是另一个故事了。

[图:修复前后的 RU 曲线,第一天]

边缘缓存也上了:wp-json 响应 TTL 24 小时(WordPress 对 REST 发 `no-store`,要用 Cache Rule 的 override_origin 无视它),发文时自动 Purge Everything 保时效。

结果:RU 从 110 降到……还有 60–120,而且波动剧烈。**第一次被打脸。**

## 第二层:日志取证,抓到扫描器,但真凶另有其人

猜不动了,开日志。给 Caddy 加上 JSON access log,看看到底什么在打源站。

证据立刻就有了:45 分钟内,**1214 个唯一的垃圾路径**——`db.old`、`pg_dump.sql`、`.env.sh`、`about.php.bak`——漏洞扫描器在扫主站,每个垃圾 URL 被 Next.js 的 catch-all 路由放大成 2–3 个 WP 查询。这解释了尖刺。

但它解释不了基线。日志同时显示一个更扎心的事实:**打到源站的请求 95% 是我自己的 Worker 发的**,而且几乎全是唯一 URL——每篇文章的 slug、每个标签页、sitemap 分页。缓存的数学在这里失效了:**缓存只能吸收重复,而爬虫是把上千个页面摊开爬的,每个 wp-json URL 在 TTL 窗口内根本等不来第二次访问**。命中率趋近于零,不管 TTL 设多长。

扫描器好办,WAF 一条规则:备份/配置文件后缀(`.sql`、`.bak`、`.env`……)直接 403,在 Worker 之前执行,零渲染零 DB。

但基线问题指向了一个更深的方向:**既然请求数压不下去,那就得压单个请求的成本。**

## 第三层:数据库视角,WordPress 的老毛病

打开 TiDB 控制台的 SQL Statements 面板,按 RU 排序,真凶终于露脸:

| SQL | 30 分钟执行次数 | 总 RU | 单次 RU |
|---|---|---|---|
| autoload 全量读(wp_options) | 880 | 26.7K | **30.3** |
| 逐条读非 autoload 选项 | **19,300** | 11.9K | 0.6 |
| taxonomy 查询 | 10,500 | 13.8K | 1.3 |
| postmeta | 9,500 | 9.6K | 1.0 |

880 就是这 30 分钟的请求数。换句话说,**每个请求都要付 100+ RU**:一次 30 RU 的 autoload 全量读、22 次逐条选项查询、12 次 taxonomy、11 次 postmeta……

这是 WordPress + 远程数据库的经典病:**WP 默认没有持久化对象缓存**,这些本该躺在内存里的配置和元数据,每个请求都从数据库重新拉一遍。数据库在本机时这病没症状,数据库换成按请求计费的远程 serverless,每一次冗余读都是真金白银。

药方是一个 100 多行的 APCu object-cache drop-in(`wp-content/object-cache.php`),让这些重复读全部走本机共享内存。

效果立竿见影:**SQL 消耗从 92K RU/30min 掉到 1.8K**,降了 98%。autoload 查询直接从榜单上消失。

[图:APCu 上线前后的 SQL Statements 对比]

RU 曲线终于断崖了:110 → 25。但是——

## 第四层:还剩 20,而且它不跟任何东西走

25 RU/s 一个月还是 65M,仍然超免费额度。继续挖,这一层最有意思:

- SQL 只占 1–3 RU/s,QPS 只有 1–5;
- `SHOW PROCESSLIST`:除了诊断连接,空无一人;
- `SHOW DATABASES`:集群上只有这一个库;
- auto-analyze:一天三四次、每次一秒;
- **RU 曲线和流量在分钟级完全解耦**——有请求时它能掉到 0,没请求时它能稳在 20。

下一个假设是「连接税」:PHP 每个请求都对 TiDB 网关新建一条 TLS 连接,会不会每条新连接的会话初始化都在计费?做了个对照实验:单连接发 300 条查询 vs 60 条新连接各发一条,看哪边在 RU 曲线上起尖。

结果:**两边都没有任何尖峰**。连接税假设阵亡。(不过顺手改的持久连接白赚一个时延收益:wp-json 从 1.3s 降到 0.3s——省掉的正是每请求的 TLS 握手和会话建立。`DB_HOST` 加个 `p:` 前缀就行。)

## 终局:静默实验

能排除的都排除了,只剩一个办法:**让集群彻底安静,看 RU 跟不跟**。

用 systemd 定时器预约 11 分钟后自动恢复(防止 SSH 断连把自己锁在外面),然后停掉 PHP-FPM。那 11 分钟里,集群上零连接、零查询,控制台也关着。

[图:静默窗口的 RU 曲线,16:43–16:55 UTC]

**RU 纹丝不动,稳稳地 20。**

谜底揭晓:这 ~20 RU/s 是 TiDB Cloud 自己的后台开销。官方 FAQ 其实写了——后台查询(节点间 schema 同步、权限刷新、SQL 绑定刷新、统计收集)会消耗 RU,控制台操作也会。只是没人告诉你它的量级是 **52M RU/月**——光是「存在」就吃掉整个免费额度。在此之前,它一直藏在我那 110 RU/s 的真实负载下面,等负载被优化到 1–3 RU/s,地板就露出来了。

## 账本与心得

最终战果:

| 指标 | 优化前 | 优化后 |
|---|---|---|
| RU 基线 | 110–120/s | ~20/s(其中可控部分 ~100 → 1–3) |
| 月消耗 | 250M+(账单 $25 量级) | ~60M(超额部分 <$1) |
| wp-json 时延 | 1.3s | 0.3s |
| RSS | 坏了一周没发现 | 正常 |

几条带走的经验:

1. **测量先于优化,而且要测对层**。我前三个假设(bot 刷接口、缓存没配好、连接太贵)全部被数据推翻。如果按直觉干,会在错误的方向上花光所有热情。
2. **缓存吸收的是重复,不是流量**。长尾唯一 URL 的场景下,命中率和 TTL 无关,出路只有两条:把请求挡在更外层,或者把单次成本打下来。
3. **WordPress 配远程计费数据库,必须上对象缓存**。APCu drop-in 一百多行,一杯咖啡的功夫,98% 的降幅。同理还有持久连接。
4. **Serverless 数据库有「存在税」**。按用量计费的承诺在后台任务面前要打折扣,选型时把这个地板算进去——对小项目来说,它可能比你的真实负载贵一个数量级。
5. **对照实验是排查的终极武器**。连接税的 A/B、停机的静默实验,每个十分钟,比任何文档都诚实。

下一步要么找 PingCAP 把这 20 RU/s 聊清楚,要么干脆把这 383MB 的库搬回 VPS 上的 MariaDB——RU 这个计费单位,从我的人生里彻底消失。

---

> 草稿备注(发布前删):
> - 所有数字来自 2026-06-11/12 的实测,截图在 TiDB 控制台和本仓库 WIP/DEV_NOTE 有据可查
> - 可以考虑在「第三层」后面贴 object-cache.php 的仓库链接(scripts/wp/object-cache.php)
> - 如果 PingCAP 工单有结果,可在文末补一节「后记」
